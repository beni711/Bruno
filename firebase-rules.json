{
  "rules": {
    ".read": false,
    ".write": false,
    "sessions": {
      "111111": {
        ".read": true,
        ".write": true,
        ".validate": "newData.hasChildren(['version', 'code', 'updatedAt', 'updatedBy', 'clearedAt']) && newData.child('version').val() === 1 && newData.child('code').val() === '111111' && newData.child('updatedAt').isNumber() && newData.child('updatedBy').isString() && newData.child('clearedAt').isNumber()"
      }
    }
  }
}
